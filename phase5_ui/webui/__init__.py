"""Web-UI und REST-API des Space-Servers (Phase 5) — eigene Cookie-Session (`__Host-sfx_session`),
kein OAuth (P5-D). Darf `storage`, `authserver` importieren, sowie genau ein Symbol aus
`mcpserver` (`permissions.OwnSpaceWritable`, P5-B).
"""

__version__ = "0.1.0"
