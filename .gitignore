# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# venv
.venv/

# derived index (Entscheidung A — nie eingecheckt)
.index.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# P4: Auth-Zustand (Clients, Codes, Token-Hashes) — autoritativ, aber nie im Repo (P4-I/V21)
*.sqlite3

# test artifacts
.pytest_cache/

# harness-local tool state (not project source)
.claude/
.agents/
package-lock.json
skills-lock.json

# opencode/M3 (P8 executor, ab Block A) — Playwright-MCP-Laufzeitartefakte
.playwright-mcp/

# machine-local deployment config (P3) — Pfade + Hostname, kein Secret, aber maschinenspezifisch
phase3_edge/local.env
