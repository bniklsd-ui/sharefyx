---
status: snapshot
purpose: Abnahmeprotokoll Phase 3 — 13 Prüfungen gegen den dauerhaft erreichbaren Dienst, mit CLI-Ausschnitt als Beleg
read-when: Abnahme von Phase 3, oder wenn jemand wissen will, was am 2026-07-27 tatsächlich funktioniert hat
detail: L2
up: ../../phase3_edge/CLAUDE.md
down:
  - ./phase3_edge_plan.md          # §5 Akzeptanzkriterien, Step 7 — dagegen wurde geprüft
updated: 2026-07-27
---
# Phase 3 — Abnahme, 2026-07-27

**Prüfling:** Sharefyx MCP-Server hinter Tailscale Funnel, als systemd-Dienst.
**Prüfer:** Nikinger (VM + Claude-Connectors), maschineller Teil über
`phase3_edge/scripts/abnahme_run.sh`.
**Ergebnis: 10 von 13 Prüfungen bestanden (Stand 2026-07-27, 21:05 CEST) — 1 offen (Reboot: 6,
Nikinger-Zeitfrage), 1 real offen (Backup-Timer: 12, akzeptiert statt abgewartet), 1 heute
negativ aus erklärtem Grund (Restore-Nachweis: 13, siehe B5), optional 14 nicht angefasst.**

> **Ausfüllhinweis, dann löschen:** Ergebnisspalte auf ✅ / ❌ / ⬜ setzen, den CLI-Ausschnitt
> unter §3 einkleben, Befunde in §4 eintragen. Der Ausschnitt kommt aus
> `./abnahme_run.sh run | tee /tmp/p3-abnahme.txt` — er ist bereits redigiert
> (Token und `/mcp/<segment>` sind ersetzt). **Trotzdem vor dem Commit einmal selbst
> durchsehen:** ein Beleg, den niemand gelesen hat, ist kein Beleg.

---

## 1 Ergebnis in vier Zeilen

1. ⬜ Die Connector-URL überlebt Reboot (offen, Nikinger) und Prozessabsturz (✅, Kill-Test
   bereits bestanden) ohne menschlichen Handgriff.
2. ✅ Beide Accounts haben einen stehenden Connector, unter echten Zwei-Nutzer-Bedingungen
   geprüft: niklas (Read+Write über `savefyx_pashe_3_test`) und fabian (Read+Write über den
   temporären `sharefyx_phase_3_fabian`-Adapter, vom Nikinger während der Session hinzugefügt).
3. ✅ Das Request-Log beantwortet Tool, Space und Dauer (10 echte Ereignisse im Fenster,
   inkl. des abgelehnten Schreibversuchs) — und enthält weder Token (Grep leer, Beleg aus dem
   09:xx-Lauf) noch Inhalt (Grep leer, dieser Lauf).
4. ⬜ Backup ist gelaufen (Bundle vorhanden), der automatisierte Restore-Check schlägt heute
   erwartbar fehl, weil das Bundle älter ist als der aktuelle HEAD — siehe B5. Mechanismus selbst
   war bereits einmal grün (`systemctl status`, Session-Archiv 2026-07-27).

---

## 2 Prüfmatrix

| # | Prüfung | Erwartung laut Plan | Ergebnis |
|---|---|---|---|
| 1 | `/health` von außen | `{"status":"ok",…,"uptime_s":N}` | ✅ |
| 2 | Connector `niklas` | ein Read + ein Write erfolgreich | ✅ echter `get_item`+`append_to_item` auf `itm_53cf4e92`, v1→v2 |
| 3 | Connector `fabian` | ein Read + ein Write im **eigenen** Space | ✅ echter `create_item`+`get_item`, `itm_2dda3690` |
| 4 | Cross-Space | fremder Body gewrappt, Schreibversuch → `write_denied` | ✅ `get_item(itm_53cf4e92)` gewrappt, `append_to_item` → `write_denied: niklas ist nicht dein Space` |
| 5 | `list_spaces` bei leerem `fabian` | eigener Space mit `item_count: 0` sichtbar (B1-Fix) | ✅ siehe §3.4 — `item_count: 0`, `writable: true`, vor Zeile 3/4 geprüft (einmalig, nicht erneut herstellbar) |
| 6 | Reboot-Test | nach `sudo reboot` ohne Handgriff erreichbar, URL unverändert | ⬜ Nikinger |
| 7 | Kill-Test | `systemctl kill -s KILL` → binnen 10 s wieder `ok` | ✅ (Vorlauf, `uptime_s: 4`) |
| 8 | Request-Log | je Tool-Aufruf `tool`, `space`, `ms` | ✅ 10 Ereignisse im Fenster, beide Spaces, inkl. `"ok":false,"err":"write_denied"` für Zeile 4 |
| 9 | Token-Grep | **leer** | ✅ (Beleg aus dem Lauf 09:xx — dieser Lauf ohne Token bewusst übersprungen) |
| 10 | Titel-/Body-Grep | **leer** | ✅ |
| 11 | Fremdzugriff | HTTP 401, Logzeile mit `/mcp/<redacted>` | ✅ 2×401 im Log |
| 12 | Backup-Timer | mindestens ein Lauf | ❌ `LastTriggerUSec` leer — Timer feuert erst 2026-07-28 00:00:35, akzeptiert |
| 13 | Restore-Nachweis | `restore_check.sh` grün, HEAD identisch | ❌ automatisierter Check heute negativ (B5, per `merge-base --is-ancestor` als reine Zeitfrage verifiziert) — Mechanismus selbst war unmittelbar nach diesem Bundle bereits `status=0/SUCCESS` |

Optional, geerbt aus P2 (`[VERIFY]` V10): Größenbudget von `search_items` gegen den echten
Bestand, sobald `fabian` Items hat. Ergebnis: ⬜

---

## 3 Belege

### 3.1 Maschineller Lauf

Aufruf: `./phase3_edge/scripts/abnahme_run.sh run </dev/null` (kein `--with-kill` — Zeile 7 bereits
bestanden, kein passwordless sudo in dieser Session; kein Token — Zeile 9 bereits aus vorigem
Lauf belegt). `--since` aus vorherigem `abnahme_run.sh start` (2026-07-27T20:39:40+02:00).

```
# P3-Abnahme — maschineller Lauf
# Datum:  2026-07-27T20:40:32+02:00
# Host:   savefyx-vmware-virtual-platform.tail89fc2a.ts.net
# Unit:   sharefyx-mcp
# --since 2026-07-27T20:39:40+02:00

[ok]      1  /health von außen  — {"status":"ok","service":"sharefyx-mcp","version":"0.1.0","uptime_s":30143}
[manuell] 2  Connector niklas: ein Read + ein Write  — in Claude fahren
[manuell] 3  Connector fabian: ein Read + ein Write im eigenen Space  — in Claude fahren
[manuell] 4  Cross-Space: fremder Body gewrappt, Schreibversuch -> write_denied  — in Claude fahren
[manuell] 5  list_spaces bei leerem fabian -> item_count 0  — in Claude fahren
[manuell] 6  Reboot-Test  — uptime seit: 2026-07-24 15:15:42
[skip]    7  Kill-Test  — nicht angefordert — erneut mit --with-kill
[ok]      8  Request-Log: 3 Tool-Ereignisse

--- Auszug (letzte 5 Tool-Ereignisse) ---
Jul 27 20:39:47 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:39:47.431Z","ev":"tool","tool":"get_item","space":"niklas","ms":27,"ok":true}
Jul 27 20:39:58 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:39:58.984Z","ev":"tool","tool":"append_to_item","space":"niklas","ms":16,"ok":true}
Jul 27 20:40:09 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:40:09.587Z","ev":"tool","tool":"search_items","space":"niklas","ms":3,"ok":true}
-----------------------------------------

[skip]    9  Token-Grep  — kein Token eingegeben
[ok]      10  Titel- und Body-Grep: leer
[ok]      11  Fremdzugriff -> 401  — 1 401-Zeilen im Log
[FEHLER]  12  Backup-Timer  — noch nie ausgelöst — 'sudo systemctl start sharefyx-backup.service' erzwingt einen Lauf
[FEHLER]  13  Restore-Nachweis  — klone /var/lib/sharefyx-backup/sharefyx-data-20260727T111252.975147Z.bundle nach /tmp/tmp.G8wFpPG040/restore ABBRUCH: Restore weicht ab — original HEAD=a400221cdb0ee79bd6900962605d0f3aa55da423 tree=9d06c07a3cb9b03e239bffff791b906279f7a3be, restored HEAD=20b66e0405ce77e5abd50cb9bf5611ba408db222 tree=4381387a6d30bbb6d119273f71f82308671f391f

# Ergebnis: 4 ok · 2 Fehler · 2 übersprungen · 5 manuell
```

Zeilen 2–5 sind im Skript grundsätzlich `[manuell]` (Connector-Tests laufen nicht über das Skript
selbst) — Zeile 2 ist unabhängig davon über den echten Connector-Aufruf belegt, siehe 3.2.

### 3.2 Connector niklas (Zeile 2)

Echter Aufruf über den `savefyx_pashe_3_test`-Adapter (Claude Code, dieser Session-Kontext) gegen
das bestehende Testitem `itm_53cf4e92` — kein neues Wegwerf-Item angelegt:

```
get_item(itm_53cf4e92)        → version: 1, Inhalt der Testmatrix-Notiz gelesen
append_to_item(itm_53cf4e92, version=1, text="[x] 2  Connector niklas: Read (get_item)
  + Write (append_to_item, dieser Eintrag) — 2026-07-27T20:40 CEST, aus Claude Code
  (savefyx_pashe_3_test Adapter)")
  → version: 2, Anhang übernommen
```

Journal-Beleg (aus dem Auszug in 3.1): `get_item … ok:true`, `append_to_item … ok:true`.

### 3.3 Reboot-Test (Zeile 6)

```
<uptime -s vor dem Reboot>
sudo reboot
<uptime -s danach>
curl -s https://<node>.<tailnet>.ts.net/health
```

### 3.4 Rule 4 unter zwei echten Nutzern (Zeilen 3, 4, 5)

Der Nikinger hat während dieser Session einen temporären Connector `sharefyx_phase_3_fabian`
hinzugefügt (echter fabian-Token). Reihenfolge eingehalten: **5 → 4 → 3.**

```
list_spaces (fabian-Connector)
  → [{"name":"fabian","item_count":0,"writable":true},
      {"name":"niklas","item_count":5,"writable":false}]
  # Zeile 5: eigener Space leer, niklas als fremder Space sichtbar und read-only

get_item(itm_53cf4e92)  # niklas-Item, über den fabian-Connector
  → <untrusted_content space="niklas"> … Inhalt der Testmatrix-Notiz … </untrusted_content>

append_to_item(itm_53cf4e92, version=2, text="Schreibversuch aus fabian — sollte abgelehnt werden.")
  → error: write_denied: niklas ist nicht dein Space; du kannst dort nur lesen
  # Zeile 4: gewrappt + abgelehnt, beides bestätigt

create_item(type=note, title="P3-Abnahme — fabian Testeintrag 2026-07-27", ...)
  → itm_2dda3690, space: fabian, version: 1
get_item(itm_2dda3690)
  → ungewrappt zurückgelesen, gleicher Inhalt
  # Zeile 3: Read + Write im eigenen Space

list_spaces (niklas-Connector, danach zur Kontrolle erneut)
  → fabian jetzt mit item_count:1, writable:false sichtbar
  # bestätigt: Cross-Space-Sichtbarkeit ist symmetrisch, sobald beide Spaces existieren —
  # das B6-Nebenbefund („fabian bei niklas unsichtbar") war nur die Henne-Ei-Lücke, dass
  # fabian als Space vor dieser Session schlicht noch nicht existierte
```

### 3.5 Request-Log (Zeile 8)

Vollständiges `--since`-Fenster (2026-07-27T20:39:40+02:00 bis zum zweiten `abnahme_run.sh
run`), alle 10 Tool-Ereignisse, beide Spaces:

```
Jul 27 20:39:47 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:39:47.431Z","ev":"tool","tool":"get_item","space":"niklas","ms":27,"ok":true}
Jul 27 20:39:58 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:39:58.984Z","ev":"tool","tool":"append_to_item","space":"niklas","ms":16,"ok":true}
Jul 27 20:40:09 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:40:09.587Z","ev":"tool","tool":"search_items","space":"niklas","ms":3,"ok":true}
Jul 27 20:56:52 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T18:56:52.364Z","ev":"tool","tool":"list_spaces","space":"niklas","ms":1,"ok":true}
Jul 27 21:03:04 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:04.666Z","ev":"tool","tool":"list_spaces","space":"fabian","ms":1,"ok":true}
Jul 27 21:03:16 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:16.612Z","ev":"tool","tool":"get_item","space":"fabian","ms":2,"ok":true}
Jul 27 21:03:25 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:25.596Z","ev":"tool","tool":"append_to_item","space":"fabian","ms":3,"ok":false,"err":"write_denied"}
Jul 27 21:03:40 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:40.951Z","ev":"tool","tool":"create_item","space":"fabian","ms":13,"ok":true}
Jul 27 21:03:47 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:47.461Z","ev":"tool","tool":"get_item","space":"fabian","ms":2,"ok":true}
Jul 27 21:03:58 savefyx-VMware-Virtual-Platform python[125404]: {"ts":"2026-07-27T19:03:58.492Z","ev":"tool","tool":"list_spaces","space":"niklas","ms":1,"ok":true}
```

`"space"` im Log ist der **Principal** (der Aufrufer), nicht das Zielobjekt — die
`append_to_item`-Zeile mit `"space":"fabian"` und `"ok":false` ist der abgelehnte
Schreibversuch aus Zeile 4 (fabian versucht, in ein niklas-Item zu schreiben).

---

## 4 Befunde

> Ein Befund ist keine Schwäche des Laufs, sondern sein Ertrag. Jeder bekommt eine Nummer,
> eine Ursache und eine Entscheidung — offen, behoben oder bewusst zurückgestellt.

B1–B4 sind nicht in diesem Dokument entstanden — B1/B2 aus der P2-Adapter-Abnahme
(`docs/concepts/P2_ADAPTER_ABNAHME_2026-07-26.md`), B3 (`sharefyx-backup.service` Permission
denied → `StateDirectory=`) und B4 (`git bundle verify` braucht Repo-Kontext → `-C
"$DATA_ROOT"`) aus der ersten P3-Live-Abnahme-Session, Commits `7368f57` und `d05464e`, Details
im Session-Archiv `phase3_edge/SESSIONS_ARCHIVE.md`. Weiter mit B5.

### B5 — Restore-Nachweis negativ, weil das Bundle älter ist als der aktuelle HEAD

**Symptom:** `restore_check.sh` (aufgerufen über `abnahme_run.sh`, Zeile 13) bricht ab: `original
HEAD=a400221c… tree=9d06c07a…`, `restored HEAD=20b66e04… tree=4381387a…` — die Bäume weichen ab.

**Ursache:** `restore_check.sh` vergleicht `HEAD`/`HEAD^{tree}` des restaurierten Bundles gegen
den **aktuellen, lebenden** `DATA_ROOT` (Skript-Zeilen 27–28), nicht gegen den Stand zum
Backup-Zeitpunkt. Das einzige vorhandene Bundle stammt von 2026-07-27T11:12:52Z; seither sind im
lebenden `DATA_ROOT` reguläre Commits entstanden — u. a. die Abnahme-Testitems selbst
(`itm_53cf4e92`, `itm_cc4866f3`) und der Read+Write-Test aus Zeile 2 dieser Session. Der
Restore-Mechanismus selbst war unmittelbar nach genau diesem Bundle bereits grün (`systemctl
status sharefyx-backup.service` zeigt für beide `ExecStart`-Prozesse `status=0/SUCCESS`, siehe
`phase3_edge/CLAUDE.md`, Session-Block 2026-07-27) — das ist kein Rückfall, sondern derselbe
Beleg wie zuvor, nur dass der *automatisierte* Check heute mit einem inzwischen veralteten Bundle
läuft.

**Verifiziert, nicht nur vermutet:** `git -C $DATA_ROOT merge-base --is-ancestor <restored_head>
<original_head>` bestätigt `true` — der restaurierte HEAD ist ein echter Vorfahre des lebenden
HEAD, keine divergente Historie. Damit ist ausgeschlossen, dass das Bundle das falsche Repo
enthält oder die Historie umgeschrieben wurde; die Abweichung ist ausschließlich Zeit, nicht
Inhalt.

**Nebenertrag — V9 live geschlossen:** `git -C $DATA_ROOT log -3` zeigt als Top-Commit `a400221c
… 2026-07-27T20:39:58+02:00 "append itm_53cf4e92 [niklas]"` — exakt der Zeitstempel des
`append_to_item`-Aufrufs aus Zeile 2 dieser Session. Der `sharefyx-mcp`-Dienst hat also unter
seiner eigenen systemd-Sandbox (`ProtectHome=read-only` + `ReadWritePaths`) tatsächlich einen
Git-Commit im `DATA_ROOT` erzeugt — damit ist das bisher offene `[VERIFY]` **V9**
(`phase3_edge/CLAUDE.md`, Runbook-Fußnote) live bestätigt und kann geschlossen werden.

**Entscheidung:** zurückgestellt, kein Restore-Defekt. Ein sauberer automatisierter ✅-Lauf
braucht ein frisches Bundle unmittelbar vor dem Check, ohne dazwischenliegende Schreibvorgänge —
das ergibt sich von selbst, sobald der Backup-Timer heute Nacht (2026-07-28T00:00:35) feuert und
`restore_check.sh` danach vor weiteren Schreibvorgängen läuft. `restore_check.sh` selbst wird
nicht angefasst (gehört nicht dieser Session).

### B6 — Fabian-Connector zunächst nicht erreichbar, dann vom Nikinger live nachgereicht

**Symptom:** Zeilen 3–5 (Connector `fabian`, Cross-Space, `list_spaces` bei leerem `fabian`)
konnten zunächst nicht durchgeführt werden — diese Session hatte anfangs nur
`savefyx_pashe_3_test` (Principal `niklas`) und ein durchgehend „not connected" gemeldetes
`sharefyx_phase_2`. `list_spaces` auf dem `niklas`-Token zeigte `fabian` zu dem Zeitpunkt auch
nicht als fremden, lesbaren Space.

**Ursache — geklärt, kein Bug:** der Nikinger hat während der Session einen dritten Connector
(`sharefyx_phase_3_fabian`, temporärer fabian-Token) hinzugefügt; nach einem Reload wurde er
sichtbar. Die anfänglich fehlende Sichtbarkeit von `fabian` im `niklas`-`list_spaces` war eine
reine Henne-Ei-Lücke: der Space `fabian` existierte zu dem Zeitpunkt schlicht noch nicht (kein
Token je ausgegeben, keine Items). Nach dem ersten `create_item` über den neuen Connector zeigt
`niklas`s `list_spaces` `fabian` korrekt als fremden, `item_count: 1`, `writable: false` — Rule 4
funktioniert wie entworfen, kein `[VERIFY]` gegen `tools.py` nötig.

**Entscheidung:** behoben — Zeilen 3, 4, 5 mit echten fabian-Aufrufen nachgeholt, Reihenfolge
5→4→3 eingehalten (siehe 3.4). Der temporäre `sharefyx_phase_3_fabian`-Connector sollte nach
Abschluss der Abnahme wieder entfernt bzw. der Token rotiert werden — er ist als „temporär"
eingeführt worden und fällt sonst unter die reguläre Token-Rotation in §6.

---

## 5 Was dieser Lauf **nicht** abdeckt

Ehrlichkeitshalber — diese Punkte bleiben an den Unit-Tests oder an späteren Phasen hängen:

| Punkt | Warum hier nicht prüfbar |
|---|---|
| Escaping des Closing-Tags | Bräuchte fremden Inhalt, der `</untrusted_content>` enthält — in fremde Spaces darf per Design niemand schreiben. Bleibt `test_wrap_untrusted_escapes_closing_tag` |
| Principal-Isolation bei Parallelität | Zwei Token gleichzeitig in einer Sitzung. Bleibt `test_principal_isolation_under_concurrency` |
| Verhalten bei Ausfall der Funnel-Relays | Nicht herstellbar. Runbook trennt „unser Dienst" von „deren Infrastruktur"; Ausweichweg dokumentiert |
| Langzeitstabilität am Mobilfunk-Uplink | Ein Abnahmelauf ist keine Woche Betrieb |

---

## 6 Abschluss

- [ ] Beide Token rotiert (`--revoke` + neu), exportiert, `systemctl restart`, Connector-URLs in
      beiden Accounts aktualisiert (P3-M)
- [ ] Testitems archiviert: `itm_53cf4e92` (Testmatrix, niklas), `itm_cc4866f3` (Titelmarker,
      niklas), `itm_2dda3690` (fabian Testeintrag, Zeile 3) und alles, was während der Zeilen
      2–4 sonst entstanden ist
- [ ] Temporärer `sharefyx_phase_3_fabian`-Connector/-Token entfernt oder in die reguläre
      Rotation überführt (B6)
- [ ] `ROADMAP.md` P3 auf ✅, `docs/INDEX.md` und `phase3_edge/CLAUDE.md` nachgezogen
- [x] Indexzeile für dieses Dokument gesetzt (2026-07-27, zweite Session):

```
- [docs/concepts/P3_ABNAHME_2026-07-27.md](./concepts/P3_ABNAHME_2026-07-27.md) — 📕 ~17KB · Abnahmeprotokoll Phase 3: 13 Prüfungen gegen den dauerhaft erreichbaren Dienst, 10/13 bestanden (Stand 2026-07-27 21:05), CLI-Ausschnitt als Beleg, Befunde B5/B6
```
