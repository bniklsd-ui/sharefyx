---
status: snapshot
purpose: Abnahmeprotokoll Phase 5 — 20 von 20 Prüfungen live bestanden (Plan §6), Matrix vollständig
read-when: Abnahme von Phase 5, oder wenn jemand wissen will, was am Ende tatsächlich live bewiesen wurde
detail: L2
up: ../../phase5_ui/CLAUDE.md
down:
  - ./phase5_ui_plan.md              # §6 Abnahmematrix — dagegen wurde geprüft, Entscheidungen P5-A–P5-AE
  - ../../phase5_ui/SESSIONS_ARCHIVE.md  # volle Belegnarrative je Zeile, newest-first
updated: 2026-08-09
---
# Phase 5 — Abnahme, 2026-08-09

**Prüfling:** Web-UI + REST-API + Auth-Selbstverwaltung (`phase5_ui/webui`) auf demselben
Storage-Kern wie die sechs MCP-Tools, hinter Tailscale Funnel, Betrieb über
Release-Verzeichnisse (`deploy.sh`/`rollback.sh`, Cutover seit 2026-08-05).
**Prüfer:** Nikinger (Browser, DevTools, `curl`, `authctl.py`, echte VM) für den überwiegenden
Teil der Matrix; Fabian als zweiter echter Nutzer (Zeile 17); Claude Code read-only gegen
`auth.sqlite3`/`journalctl`/den echten MCP-Connector als zweite, unabhängige Quelle bei mehreren
Zeilen (14, 17, 20) statt bloßer Übernahme der gemeldeten Ergebnisse.
**Ergebnis: 20 von 20 Prüfungen live bestanden, 0 teilweise, 0 offen.** Kein Fehlschlag in der
Matrix. Die Ergebnisse entstanden über sieben Sessions verteilt (2026-08-04 bis 2026-08-09), die
volle Narrative je Zeile steht in `phase5_ui/SESSIONS_ARCHIVE.md` (newest-first) — dieses
Dokument fasst zusammen, verweist, erfindet keine zweite Quelle der Wahrheit.

> **Statusregel des Plans (unverändert übernommen):** ✅ heißt live-verifiziert, nicht gebaut.
> Diese Regel gilt für jede der 20 Zeilen unten — Code-fertig war für sechs Zeilen (10, 13, 14,
> 15, 17, 19) schon vor dem 2026-08-07-Sprung auf „live" der Fall, zählte bis dahin bewusst
> nicht als bestanden.

---

## 1 Ergebnis in fünf Zeilen

1. ✅ Block A (Sicherheit/Selbstverwaltung) ist vollständig live bewiesen: Einladung, Passwort,
   TOTP, Recovery-Code, Fehlversuchsbremse, Session-Isolation (Zeilen 1–9) — inklusive eines
   scharfen Fundes am Gate selbst (Origin/Referrer-Policy, Zeile 3, siehe §4).
2. ✅ Block B (REST-API/UI) hält Hard Rule 3 (Versionsband) und Hard Rule 4 (fremde Spaces
   read-only, Body gewrappt) unter echter Browser-Last, nicht nur unter `pytest` (Zeilen 10–13).
3. ✅ Die Zwei-Wege-Trennung `/mcp` (nur Bearer) vs. `/api`+`/ui` (nur Cookie) hält live gegen
   gefälschte Gegenprobe-Requests, nicht nur in `test_isolation.py` (Zeile 19).
4. ✅ Betrieb: `deploy.sh` rollt bei kaputtem Health-Gate automatisch zurück, `ui_budget.py`
   liegt im Zielkorridor, und ein realer, unbewusst ausgelöster Reboot bewies live, dass Dienst,
   Connector, UI und Timer ohne Handgriff zurückkommen (Zeilen 15, 16, 20).
5. ✅ Beide Nutzer (Niklas, Fabian) benutzten UI **und** Connector am selben Tag gegen dieselbe
   Instanz — das eigentliche Ziel der Phase (Zeile 17). Ein Nebenfund dabei (S10 unter
   Live-Bedingungen noch nicht durch einen echten Reset **nach** Deploy ausgelöst) bleibt
   dokumentiert, ohne Zeile 17 selbst zu berühren (§4).

---

## 2 Prüfmatrix (Plan §6)

| # | Kriterium | Ergebnis | Beleg (Kurzform — voll in `phase5_ui/CLAUDE.md`/Archiv) |
|---|---|---|---|
| 1 | Einladungslink erzeugt, Konto von null auf aktiv | ✅ | Nikinger live, 2026-08-04 |
| 2 | Einladungslink ein zweites Mal → abgelehnt | ✅ | Nikinger live, 2026-08-05 |
| 3 | TOTP-Seed einmal gezeigt, Authenticator-Code akzeptiert | ✅ | Nikinger live, 2026-08-04 — nach dem Origin/`Referrer-Policy`-Fund (§4) |
| 4 | Recovery-Code ersetzt den TOTP-Code, danach abgelehnt | ✅ | Nikinger live, 2026-08-05 |
| 5 | Passwortwechsel ohne `systemctl restart`, neuer Login sofort gültig | ✅ | Nikinger live, 2026-08-05 — schließt Betriebsnotiz O1 auch live |
| 6 | Passwortwechsel: Connector neu autorisieren · fremde UI-Sitzung beendet · eigene läuft weiter | ✅ | Nikinger live, 2026-08-05; DB-gegengeprüft: 7× `password_changed` (Sessions+Familien), 6× `rotated` (eigene Sitzung) |
| 7 | Fehlversuchsbremse für UI-Login **und** OAuth-Consent gemeinsam | ✅ | Nikinger live, 2026-08-05 |
| 8 | `authctl.py list-users` zeigt keinen Hash, keinen Seed | ✅ | Nikinger live, 2026-08-05 |
| 9 | `auth.sqlite3` mit `strings`: kein Base32-Seed im Klartext | ✅ | Nikinger live, 2026-08-05 |
| 10 | Anlegen/Bearbeiten/Anhängen/Archivieren über UI; `.md` + Git-Commit korrekt | ✅ | Nikinger live, 2026-08-07 — vier UI-Aktionen, vier eigene Commits im `DATA_ROOT` |
| 11 | Konflikt in zwei Tabs → Versionsband-Warnung + Dialog, kein stiller Überschreiber | ✅ | Nikinger live, 2026-08-05 nach Step 7b |
| 12 | Fremder Space sichtbar/lesbar, ohne Schreib-Bedienelemente im DOM | ✅ | Nikinger live in DevTools, 2026-08-05 |
| 13 | Unbekanntes Frontmatter-Feld überlebt eine UI-Bearbeitung unverändert | ✅ | Nikinger live, 2026-08-07 — Nebenfund: Drift-Erkennung (`store.py`) schrieb einen eigenen Repair-Commit vor dem UI-Update, kein Bug |
| 14 | `format: markdown` erscheint nach erstem UI-Schreibvorgang, stört keinen Tool-Aufruf | ✅ | Claude Code live über den echten MCP-Connector (`get_item`, Space `niklas`) gegengeprüft |
| 15 | `ui_budget.py` liefert alle vier Zahlen im Zielkorridor | ✅ | Nikinger live, 2026-08-07, mit `.venv/bin/python` (falscher Interpreter beim ersten Versuch, kein Befund) |
| 16 | `deploy.sh` rollt bei kaputtem Health-Endpunkt automatisch zurück | ✅ | Nikinger live, 2026-08-05 20:40 — simulierter toter Port, `.failed`-Markierung griff |
| 17 | Beide Nutzer benutzen UI **und** Connector am selben Tag gegen dieselbe Instanz | ✅ | Nikinger + Fabian, 2026-08-07 — DB-gegengeprüft statt Meldung übernommen; Nebenfund zu S10 siehe §4 |
| 18 | `git diff` auf `storage/`, `mcpserver/{tools,permissions,server}.py`: leer | ✅ | bei jedem Step-Commit geprüft, zuletzt Step 7b |
| 19 | Cookie an `/mcp` ignoriert; Bearer an `/api` ignoriert | ✅ | Testsuite + Nikinger live, 2026-08-07: `curl` gegen `PUBLIC_BASE_URL`, beide Richtungen `401` |
| 20 | Reboot: UI, Connector, Timer kommen ohne Handgriff zurück | ✅ | Nikinger live, 2026-08-09 — unbewusster VM-Reboot, gleicher Prüffall wie P3 Zeile 6, Details §3 |

**20 von 20 bestanden, 0 teilweise, 0 offen.** Kein Fehlschlag in dieser Matrix.

---

## 3 Beleg im Detail — Zeile 20 (dieser Sessionabschluss)

Read-only erhoben, kein Handgriff, Prüfmaßstab identisch zu P3 Zeile 6
(`phase3_edge/CLAUDE.md`, Korrekturnotiz 2026-07-29):

| Check | Ergebnis |
|---|---|
| Boot-Zeit | `uptime -s` → `2026-08-09 11:40` |
| `sharefyx-mcp.service` | `enabled`; `ExecMainStartTimestamp` `11:40:20` — 0s nach Boot, kein manueller `start`/`restart` im Journal |
| Connector | authentifizierter MCP-Traffic um `11:42:08` (`ListResources`/`ListPrompts`/`ListTools`, je `200`); zusätzlich `list_spaces` live über den echten, produktiven Connector aufgerufen (`fabian`: 6 Items ro, `niklas`: 40 Items rw) — sauberer Read, kein Re-Auth nötig |
| UI | `/ui/login` lokal (Port `8765`) **und** über die öffentliche Funnel-URL → beide `200`; `/api/v1/me` und `/mcp/` ohne Auth → beide `401` |
| Tailscale Funnel | `tailscaled` startet zeitgleich mit dem MCP-Dienst (`11:40:20`), Funnel-Konfiguration übersteht den Reboot ohne erneutes `tailscale funnel` |
| Timer (purge/backup/authbackup) | alle drei `enabled`, `list-timers` zeigt für alle drei eine gültige `Next`-Zeit — kein Timer ist seit dem Boot gefeuert, das verlangt die Zeile auch nicht (siehe P3 Zeile 6 vs. 12, dieselbe Trennung „kommt zurück" vs. „ist gelaufen") |

Für die Zeilen 1–19 gilt: volle Belegnarrative bereits zum jeweiligen Zeitpunkt im Session-Block
dokumentiert, seither in `phase5_ui/SESSIONS_ARCHIVE.md` — hier bewusst nicht dupliziert.

---

## 4 Befunde und Vormerkungen (keine offenen Blocker)

Diese Abnahme selbst hat keinen neuen Fehlschlag erzeugt. Vier Punkte aus dem Phasenverlauf
bleiben als dokumentierte Fakten stehen, keiner davon hält eine Matrixzeile offen:

- **Origin/Referrer-Policy-Fund (Block-A-Gate, 2026-08-04):** vor Zeile 3 gefunden und noch in
  derselben Session behoben — Details `phase5_ui/SESSIONS_ARCHIVE.md`, Session-Block 2026-08-04.
- **S9** (gefunden + geschlossen, Step 4) und **S10** (gefunden 2026-08-06, Fix `e760f0e`
  10:33, deployt 2026-08-07 05:23/Restart 07:25) — Kurztabelle mit vollem Verlauf in
  `phase4_auth/CLAUDE.md`. **Nebenfund dieser Abnahme:** Fabians aktive `token_families`-Zeile
  vom 2026-08-07 stammt aus einer Autorisierung **vor** dem S10-Fix (2026-08-06 08:23) — S10
  ist im Code bewiesen (`test_invite_reset_revokes_old_token_families_and_sessions` grün, Commit
  Vorfahr des deployten Release) und im Live-System aktiv, aber noch nicht durch einen echten
  Reset **nach** dem Deploy ausgelöst worden. Keine Regression, deckt sich mit dem offenen
  **O2** (`clients`/`token_families` werden nie abgeräumt).
- **F1/F2** (Nikinger-Meldung, Step 8b) — Subspaces/eigene Ordner + vollständiges Löschen,
  bewusst nicht in dieser Phase umgesetzt (F1b kollidiert mit Hard Rule 4). Phase-6-Vormerkung,
  Root-`CLAUDE.md` „Current state".
- **`patch_item`-Werkzeug-Feedback** (2026-08-08, aus einer arbeitenden Claude-Instanz über den
  echten Connector) — `update_item` ersetzt immer den ganzen Body, teuer bei kleinen Korrekturen
  an großen Dokumenten. Berührt `mcpserver/tools.py` (P5-B tabu für diese Phase), Kandidat für
  denselben Phase-6-Zuschnitt wie F1/F2. Voller Wortlaut: Root-`CLAUDE.md`.

Alle vier sind Phase-6-Material oder bereits geschlossen — keiner davon ist Voraussetzung für
den formalen Abschluss dieser Phase.

---

## 5 Abschluss

- [x] Alle 20 Abnahmezeilen ✅ — `phase5_ui/CLAUDE.md` §„Abnahmestand" nachgezogen (2026-08-09)
- [x] `docs/INDEX.md` um dieses Dokument ergänzt, Größen/Abnahmestand-String nachgezogen
- [x] Session-Block rotiert (`scripts/rotate_session_block.sh phase5_ui`) — Head unter dem
      40 KB-Softcap
- [ ] Root-`CLAUDE.md`/`ROADMAP.md` auf „Phase 5 ✅" — **bewusst noch nicht gesetzt**, hängt an
      den übrigen Step-9-Abschlussarbeiten (Runbook-Schritt 4, `phase5_ui_uebersicht.svg`,
      `PHASE5_CLOSEOUT_HANDOVER.md`), nicht an dieser Matrix allein
- [x] Migrations-Runbook-Schritt 4 (`auth-users`-Credential/Keyring entfernen) — **bereits
      erledigt vorgefunden**, nicht Teil dieser Session: die live installierte Unit trägt keine
      `LoadCredentialEncrypted=auth-users`-Zeile mehr (nur noch `auth-dek`), `/etc/sharefyx/`
      enthält keine `auth-users.cred`-Datei, und der `nikinger-space`/`auth-users`-Keyring-
      Eintrag existiert nicht mehr (read-only geprüft, `keyring.get_password(...)` → `None`).
      Die Laufzeit (`flows.py`/`app.py`) liest ohnehin nur noch über `UserDirectory`/
      `auth.sqlite3`, `users.py`s `load_users_from_keyring()` wird von keinem laufenden Prozess
      mehr aufgerufen (nur von den Admin-Skripten `provision_user.py`/`export_auth_users.py`,
      die dieses Wissen nicht mehr benötigen). Kein Kommando nötig, kein Risiko für Fabians
      aktive Sitzung — nichts zu tun.

**Offen für den nächsten Schritt:** `phase5_ui_uebersicht.svg` und
`PHASE5_CLOSEOUT_HANDOVER.md` — laut Nikinger-Planung (2026-08-09) bisher Aufgabe der
Browser-Planungssession, mit Absicht das noch zu ändern; Details dazu im nächsten Zuschnitt,
nicht Teil dieser Session.
