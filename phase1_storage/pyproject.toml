[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "storage"
version = "0.1.0"
description = "Storage-Kern des Space-Servers (Phase 1) — Dateien + Index + Versionierung"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
packages = ["storage"]
